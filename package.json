{
  "name": "sdo-ne",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:init": "wrangler d1 execute arta-db --local --file=./schema.sql",
    "db:init:remote": "wrangler d1 execute arta-db --remote --file=./schema.sql",
    "cf-typegen": "wrangler types --env-interface Env"
  },
  "dependencies": {
    "hono": "^4.13.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260804.1",
    "typescript": "^5.9.3",
    "wrangler": "^4.119.0"
  }
}
